# Secrets / local config (commit only sanitized *.example files)
.env
.env.local
.env.*.local
.envrc
*.pem
*.key
*.p12
*.keystore
id_ed25519*
id_rsa*

# Logs / OS
*.log
.DS_Store

# Local contributor specs (kept out of new clones)
AGENTS.md
spec.md

# Local devcontainer (keep local only)
.devcontainer/

# Local data (keep demo state out of git)
data/
*.sqlite
*.sqlite3
*.db
events.jsonl
events.jsonl.*

# Node / Next
node_modules/
.pnpm-store/
dist/
build/
.next/
.turbo/

# Foundry (if/when added)
out/
cache/
broadcast/
harness/v4-hook-harness/lib/
